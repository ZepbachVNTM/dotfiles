source $ZDOTDIR/zshrc

# fnm
export PATH=/home/zepbachvntm/.fnm:$PATH
eval "`fnm env`"

# fnm
export PATH=/home/zepbachvntm/.fnm:$PATH
eval "`fnm env`"

# fnm
export PATH=./.fnm:$PATH
eval "`fnm env`"
